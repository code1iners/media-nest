services:
  mytube-extract:
    build:
      context: .
    image: mytube-extract:latest
    container_name: mytube-extract
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "127.0.0.1:3030:3030"
