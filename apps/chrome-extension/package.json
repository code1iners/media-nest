{
  "name": "chrome-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Media Nest Chrome extension source snapshot.",
  "scripts": {
    "build": "node -e \"const fs=require('fs'); fs.accessSync('manifest.json'); fs.mkdirSync('dist',{recursive:true}); fs.writeFileSync('dist/.build-snapshot','verified\\n')\"",
    "lint": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\""
  }
}
